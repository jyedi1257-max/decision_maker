# 작업 규칙

이 저장소는 기준문서 두 개가 먼저 있고 코드가 그 뒤를 따른다.
화면이나 문구를 새로 만들기 전에 둘 중 맞는 쪽을 먼저 본다.

- [`docs/design-system.md`](docs/design-system.md) — 색·서체·모션·컴포넌트. **여기 없는 값을 새로 만들지 않는다.**
  필요하면 문서에 먼저 추가한다.
- [`docs/product-report.md`](docs/product-report.md) — 무엇을 만들고 무엇을 안 만드는지, 그 근거.

## 절대 하지 않는 것

디자인 시스템 §9:

- 확신도·만족도에 `--accent` 외의 새 색 쓰기
  ("앞선 선택지"는 2026-09-20부터 §1의 선택지 고유색 `--alt-1`~`--alt-5`를 쓴다.
  앞선 쪽은 100%, 나머지는 `--alt-dim`. 그 다섯 색 밖으로 나가지 않는다.)
- 손글씨 요소에 자간 조정 넣기
- 900ms를 넘는 진입 모션
- **총점 숫자를 기본 화면에 노출** — 적합도는 `낮음/보통/높음`과 막대로만
- 반복 루프 애니메이션
- 다크모드

900ms 예외는 매트릭스 화면의 필기 모션 하나뿐이다 (디자인 시스템 §9).
건너뛰기·`prefers-reduced-motion`·소리 끄기가 셋 다 있어야 성립한다.

블록마다 작은 표제부를 얹지 않는다 (2026-09-20). 라벨 자리에는 **문장**을 넣고
카드 테두리 대신 여백과 실선으로 끊는다 — `.block` / `.say`가 그 틀이다.

기획안 8.3 (AI 역할):

- AI가 점수를 매기기 — "근거 없이 AI가 8점"
- AI가 최종 선택을 단정
- AI로 가중치를 뽑기 — ROC가 결정론적이어야 결과를 기준별로 분해할 수 있다

## 경계

`src/core/`는 순수 TypeScript다. React도 Firebase도 DOM도 import하지 않는다.
결정의 옳고 그름이 걸린 코드는 전부 여기 있고, 테스트도 여기만 붙는다.

`src/core/advisor.ts`의 `Advisor` 인터페이스가 AI가 닿을 수 있는 유일한 지점이다.
점수·선택·가중치를 이 인터페이스에 추가하지 않는다.

## 손글씨 문구를 고칠 때

`src/styles/ink-phrases.ts`의 `INK_PHRASES`나 `INK_GLYPHS`를 고쳤으면 반드시:

```bash
npm run font:subset
```

앱에 실린 폰트는 그 글자만 담은 서브셋이다. 빼먹어도 글자가 깨지지는 않지만
(`unicode-range` 폴백) 새 문구가 조용히 본문 서체로 나온다 — 알아채기 어렵다.
스크립트가 woff2와 `base.css`의 `unicode-range`를 함께 갱신하니 손으로 옮겨 적지 않는다.

선택지·기준 **이름은 손글씨로 쓰지 않는다.** 사용자가 직접 치는 글자라 서브셋에 담을 수 없다.

## 앱 아이콘을 고칠 때

`scripts/icon-source.mjs`가 유일한 원본이다. 고쳤으면 반드시:

```bash
npm run icons
```

안드로이드 15장(5밀도 × 런처·원형·적응형 앞면) + 스플래시 11장 + 웹 6장 +
적응형 배경색이 한 번에 다시 나온다. PNG를 손으로 만들면 어느 게 최신인지
알 수 없게 되고, 밀도 하나를 빠뜨려도 아무도 모른다.

스크립트가 굽기 전에 **모티프가 안전 영역 안에 있는지 픽셀로 잰다.**
넘으면 굽지 않고 멈춘다 — 원형 마스크를 씌우는 런처에서 잘리면 되돌릴 방법이 없다.

손글씨는 앱에 실린 `penink.woff2`를 그대로 쓴다. 문구를 바꾸면 그 글자가
서브셋에 있는지 먼저 확인한다(`INK_GLYPHS`).

## 스티커를 고칠 때

`scripts/make-stickers.mjs`가 유일한 원본이다. 고쳤으면 반드시:

```bash
npm run stickers
```

`src/assets/stickers/`의 SVG, `index.json`, 피그마용 `figma-sheet.svg`가 통째로 다시 나온다.
SVG를 손으로 고치지 않는다. 스티커마다 `외곽선 · 칠 · 무늬 · 먹선` 레이어가 나뉘어 있어야 한다 — 피그마에서 객체를 따로 쓰기 위해서다.
무늬는 SVG `pattern`으로 채우지 않는다. 피그마가 제대로 못 읽어서, 실제 점·선을 그림 모양으로 잘라 넣는다.

색: 그림·라벨은 먹·흰색·회색 종이만. 포스트잇·마스킹테이프는 디자인 시스템 §1의 '스티커 전용 감성 컬러'만.
감성 컬러는 스티커로 붙일 때만 화면에 나온다 — 버튼·배지·막대·글자의 색으로는 쓰지 않는다.

화면에 붙이는 자리는 디자인 시스템 §5 '스티커 (화면)'에 정해져 있다. 결과·자세히·조정해보기·내가 매긴 표·확정에는 붙이지 않는다.

## 안드로이드

세션이 열릴 때 `.claude/hooks/session-start.sh`가 npm 의존성과 안드로이드 SDK를
갖춰 둔다. 이미 깔려 있으면 건너뛰므로 두 번째부터는 몇 초다.

```bash
npm run build && npx cap sync android
cd android && ./gradlew assembleDebug     # app/build/outputs/apk/debug/
```

SDK 버전은 `android/variables.gradle`의 `compileSdkVersion`을 훅이 읽어 따라간다.
거기를 올리면 다음 세션에 맞는 걸 받는다 — 훅을 따로 고칠 필요 없다.

에뮬레이터는 이 컨테이너에서 못 돌린다(`/dev/kvm` 없음). 켜서 화면이 뜨는지는
실기기나 CI 밖에서 확인해야 한다.

`google-services.json`은 필요 없다. 앱은 WebView 안에서 Firebase **웹** SDK를 쓰고,
`android/app/build.gradle`이 그 파일이 있을 때만 플러그인을 적용한다.

## 검증

```bash
npm test          # 결정 엔진
npm run build     # 타입 + 빌드
npm run build && npm run smoke   # 전체 화면 완주 + 스크린샷
```

`npx tsc --noEmit`은 아무것도 검사하지 않는다 (`tsconfig.json`이 프로젝트 참조만 갖고 있다).
타입은 `npm run build`로 본다.

스모크는 화면마다 `docs/screenshots/`에 스크린샷을 남긴다. 화면을 고쳤으면
스크린샷도 함께 갱신해 커밋한다.

## 문장을 쓸 때

기획안 8.1 원칙 7 — "정답/최선"으로 단정하지 않는다.
"현재 입력 기준에서는 A가 더 잘 맞는다"라고 쓴다.

민감도는 그래프보다 문장이 먼저다 (§6.7). 숫자와 그래프는 '왜 이런 결과인지' 화면 안에만 둔다.

회고 패턴 같은 통계 문장은 **표본이 충분할 때만** 내보낸다. 한 건으로 경향을 지어내지 않는다.
