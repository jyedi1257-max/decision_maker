# 작업 규칙

이 저장소는 기준문서 두 개가 먼저 있고 코드가 그 뒤를 따른다.
화면이나 문구를 새로 만들기 전에 둘 중 맞는 쪽을 먼저 본다.

- [`docs/design-system.md`](docs/design-system.md) — 색·서체·모션·컴포넌트. **여기 없는 값을 새로 만들지 않는다.**
  필요하면 문서에 먼저 추가한다.
- [`docs/product-report.md`](docs/product-report.md) — 무엇을 만들고 무엇을 안 만드는지, 그 근거.

## 절대 하지 않는 것

디자인 시스템 §9:

- 확신도·만족도·"앞선 선택지"에 `--accent` 외의 새 색 쓰기
- 손글씨 요소에 자간 조정 넣기
- 900ms를 넘는 진입 모션
- **총점 숫자를 기본 화면에 노출** — 적합도는 `낮음/보통/높음`과 막대로만
- 반복 루프 애니메이션
- 다크모드

기획안 8.3 (AI 역할):

- AI가 점수를 매기기 — "근거 없이 AI가 8점"
- AI가 최종 선택을 단정
- AI로 가중치를 뽑기 — ROC가 결정론적이어야 결과를 기준별로 분해할 수 있다

## 경계

`src/core/`는 순수 TypeScript다. React도 Firebase도 DOM도 import하지 않는다.
결정의 옳고 그름이 걸린 코드는 전부 여기 있고, 테스트도 여기만 붙는다.

`src/core/advisor.ts`의 `Advisor` 인터페이스가 AI가 닿을 수 있는 유일한 지점이다.
점수·선택·가중치를 이 인터페이스에 추가하지 않는다.

## 손글씨 문구를 고칠 때

`src/styles/ink-phrases.ts`를 고쳤으면 반드시:

```bash
npm run font:subset
```

앱에 실린 폰트는 그 문구의 글자만 담은 서브셋이다. 빼먹어도 글자가 깨지지는 않지만
(`unicode-range` 폴백) 새 문구가 본문 서체로 나온다.

## 검증

```bash
npm test          # 결정 엔진
npm run build     # 타입 + 빌드
npm run build && npm run smoke   # 13화면 완주 + 스크린샷
```

스모크는 화면마다 `docs/screenshots/`에 스크린샷을 남긴다. 화면을 고쳤으면
스크린샷도 함께 갱신해 커밋한다.

## 문장을 쓸 때

기획안 8.1 원칙 7 — "정답/최선"으로 단정하지 않는다.
"현재 입력 기준에서는 A가 더 잘 맞는다"라고 쓴다.

민감도는 그래프보다 문장이 먼저다 (§6.7). 숫자와 그래프는 '왜 이런 결과인지' 화면 안에만 둔다.

회고 패턴 같은 통계 문장은 **표본이 충분할 때만** 내보낸다. 한 건으로 경향을 지어내지 않는다.
