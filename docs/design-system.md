---
title: 의사결정 지원 앱 — 디자인 시스템
status: 확정
date: 2026-09-19
prototype: https://claude.ai/artifact/HzEGb1Ki3S3g34THM5x52D
---

# 의사결정 지원 앱 디자인 시스템

> 팔레트 A(원고지) 기준 확정. 이 문서가 단일 기준이다. 화면을 새로 만들 때 여기 없는 값을 새로 만들지 말고, 필요하면 이 문서에 먼저 추가한다.

---

## 1. 색

```css
:root{
  --desk:    #EFECE5;  /* 종이 바깥 책상 */
  --paper:   #FAF9F6;  /* 종이 배경 */
  --rule:    #EAE7DF;  /* 괘선(가로줄) */
  --card:    #FFFFFF;  /* 카드 배경 */
  --border:  #E3DFD5;  /* 카드 테두리 */
  --dashed:  #D2CCBF;  /* 점선 테두리(추가 버튼 등) */
  --ink:     #23201C;  /* 본문, 확정 버튼 배경 */
  --soft:    #5A554C;  /* 설명문 */
  --meta:    #6E695C;  /* 보조 텍스트·날짜·타이머 (2026-09-19 대비 미달로 #8A8478→#6E695C 수정) */
  --pen:     #B3402B;  /* 빨간 펜 — 사용자의 손자국 전용 */
  --pen-hover: #8E3120;
  --accent:  #2F6A5E;  /* 확신도·만족도·적합도 앞선 쪽 — 단일 강조색으로 통일 */
  --hl:      #F2D98D;  /* 형광펜, 화면당 최대 1곳 */
  --postit:  #F2D98D;  /* 포스트잇 (2026-09-24) — 값은 --hl과 같고 역할만 다르다. 홈 목록의 '손이 가야 하는' 상태 쪽지 */

  /* 선택지 고유색 (2026-09-20 추가) — ①②③④⑤가 각자 갖는 색.
     비교 화면에서 순위가 바뀔 때 "무엇이 무엇을 제쳤는지" 눈으로 따라가게 한다. */
  --alt-1:   #B3402B;  /* = --pen */
  --alt-2:   #2F6A5E;  /* = --accent */
  --alt-3:   #78591F;
  --alt-4:   #4A6280;
  --alt-5:   #6E695C;  /* = --meta */
  --alt-dim: 0.55;     /* 앞서지 않은 쪽 막대의 불투명도 */
}
```

### 스티커 전용 감성 컬러 (2026-09-25)

포스트잇·마스킹테이프 스티커에만 쓴다. 스티커로 화면에 붙이는 건 되지만, **버튼·배지·막대·글자 같은 화면 요소의 색으로는 쓰지 않는다** — 그 색은 위 팔레트뿐이다.
바탕색과 한 톤 짙은 무늬색이 짝이다.

| 이름 | 바탕 | 무늬 |
|---|---|---|
| 분홍 | `#EFC4BB` | `#D99F94` |
| 세이지 | `#C6D4B7` | `#A1B58F` |
| 버터 | `#F4E1A3` | `#E0C270` |
| 하늘 | `#C3D6E8` | `#95B3D1` |
| 라벤더 | `#D6CBE8` | `#AE9FD0` |

### 색 사용 규칙

| 색 | 쓰는 곳 | 절대 안 쓰는 곳 |
|---|---|---|
| `--ink` | 본문, 제목, 확정 버튼(검정 배경) | 강조 표시 |
| `--pen` | 밑줄·체크·동그라미·취소선·도장·손글씨 메모, ① 선택지 색(`--alt-1`) | 상태 배지·상태 글자("손해", "바꿈"), 버튼 배경, 링크, 포커스 테두리 |
| `--accent` | 확신도 버튼, 만족도 버튼, 결과에서 앞선 선택지 막대·아이콘, "통과" 배지 | 링크, 제목 |
| `--hl` | 결과 화면의 결론 문장 1곳 | 여러 곳 동시에 |
| `--postit` | 홈 목록 카드의 상태 쪽지 중 손이 가야 하는 것("회고할 때", "진행 중") | 홈 밖, 형광펜 대신 |

- **확신도·만족도는 전부 `--accent` 하나로 통일한다.** 별도 색을 만들지 않는다.
- **"앞선 선택지"는 2026-09-20부터 예외다.** 선택지마다 `--alt-1`~`--alt-5`를 갖고, 앞선 쪽은 그 색을 그대로, 나머지는 `--alt-dim`(55%)으로 흐리게 칠한다. 앞선 쪽이 눈에 띄어야 한다는 원래 의도는 그대로다 — 방법만 "혼자 색이 있음"에서 "혼자 진함"으로 바뀐다.
  - 쓰는 곳은 **선택지를 나란히 비교하는 화면뿐**이다: 결과 · 직접 조정해보기 · 내가 매긴 표. 직감·필수조건·평가·확정·회고에서는 `--pen`을 그대로 쓴다. 앱 전체를 색으로 칠하면 `--pen`이 "사용자의 손자국"이라는 뜻을 잃는다.
  - 색은 **선택지 번호에 붙는다**(①은 항상 `--alt-1`). 순위가 아니라 정체성이다.
- `--pen`과 `--accent`는 역할이 다르다: 펜은 "사용자가 쓴 흔적", 강조는 "계산 결과가 가리키는 쪽".
- **빨강은 오류로 읽힌다 (2026-09-23).** 그래서 사용자의 손자국이 아닌 곳에는 쓰지 않는다.
  - 포커스 테두리·글자 링크는 `--ink`(링크는 `--rule-ink` 밑줄).
  - 결과의 "적합도 높음" 글자는 **제 막대와 같은 색**(`--alt-n`)이다. 막대는 빨강인데 글자는 초록이면 서로 다른 말을 한다.
  - 차이를 만든 기준은 좋다/나쁘다 색으로 칠하지 않는다 — 앞선 쪽에 유리하면 `--ink`, 불리하면 `--soft`와 아래 화살표.
  - 예외는 한 곳: 보관함의 '전부 지우기' 확인. 되돌릴 수 없는 일이라 경고로 읽혀야 한다.
- 다크모드는 지원하지 않는다.

### 접근성 대비 (2026-09-19 검증)

| 조합 | 대비 | 기준 | 결과 |
|---|---|---|---|
| `--ink` / `--paper` | 15.40:1 | 4.5:1 | 통과 |
| `--ink` / `--card` | 16.22:1 | 4.5:1 | 통과 |
| `--soft` / `--paper` | 7.03:1 | 4.5:1 | 통과 |
| `--meta` / `--paper` | 5.20:1 | 4.5:1 | 통과 (수정 후) |
| `--pen` / `--paper` | 5.41:1 | 4.5:1 | 통과 |
| `--accent` / `--paper` | 5.96:1 | 4.5:1 | 통과 |
| `--rule` / `--paper` (장식선) | 1.17:1 | 3:1 | 미달이나 비텍스트 장식 요소로 예외 |

### 선택지 고유색 대비 (2026-09-20 검증)

막대는 비텍스트 그래픽 요소라 3:1, 번호·이름은 글자라 4.5:1이 기준이다.

| 색 | 막대 / `--rule` | 글자 / `--paper` | 결과 |
|---|---|---|---|
| `--alt-1` #B3402B | 4.61:1 | 5.41:1 | 통과 |
| `--alt-2` #2F6A5E | 5.08:1 | 5.96:1 | 통과 |
| `--alt-3` #78591F | 5.23:1 | 6.14:1 | 통과 (후보 #8A6B2F가 4.72:1로 여유가 없어 어둡게 수정) |
| `--alt-4` #4A6280 | 5.07:1 | 5.95:1 | 통과 |
| `--alt-5` #6E695C | 4.43:1 | 5.20:1 | 통과 |

앞서지 않은 막대(`--alt-dim` 55%)는 트랙 대비 1.88~2.05:1로 3:1에 미달한다. 다만 이는
**지금 쓰는 `--bar-rest`(1.84:1)와 같은 수준**이고, 길이로 값을 읽는 보조 막대라 의도적으로
낮춘 것이다. 앞선 쪽은 위 표대로 전부 통과한다.

---

## 2. 타이포그래피

| 용도 | 폰트 | 굵기 | 자간 |
|---|---|---|---|
| 제목·질문 문장·입력 필드 | Gowun Batang | 700 | 기본값(조정 없음) |
| 본문·버튼·라벨 전체 | IBM Plex Sans KR | 400 / 500 / 600 | 기본값 |
| 손글씨(사용자의 흔적) | **KCC김환기체** | 400 | **조정 없음** |

### 손글씨 폰트 — KCC김환기체

- **출처**: 한국저작권위원회 공유마당, 김환기 화백의 손글씨를 기증받아 개발
- **라이선스**: OFL(Open Font License) — 폰트 파일 자체를 유료 판매하지 않는 한 자유 이용·수정·재배포 가능
- **적용 범위**: `direction`, `처음 마음은 ①`, `여기부터 ①`, `결정함`, `이런 문장도 좋아요`, `세 개면 충분해요`, 회고 화면의 인용구, **매트릭스 화면의 점수·①②③·무게 비율·적합도 라벨** — 사용자가 직접 남긴 것처럼 보이는 요소에만 사용. 자간은 원본 그대로 둔다.
- **임베딩 방식**: 전체 폰트(12,264자)를 그대로 불러오지 않고, 화면에 실제로 쓰이는 글자만 골라 woff2로 서브셋한 뒤 `@font-face`에 base64로 직접 삽입. 외부 폰트 서버 요청이 없어 로딩 실패 위험이 없다.
- **낱글자 서브셋** (2026-09-20 추가): 문구뿐 아니라 `0-9`, `%`, `①②③④⑤`, `낮음/보통/높음`도 서브셋에 담는다. 매트릭스 화면에서 **사용자가 직접 매긴 점수**를 손글씨로 찍기 위해서다 — 이것도 "사용자가 직접 남긴 것"이라 §2의 적용 범위 안에 있다.
  선택지·기준 **이름은 손글씨로 쓰지 않는다.** 사용자가 직접 치는 글자라 어떤 글자가 나올지 알 수 없고, 전부 담으려면 원본 1.9MB를 통째로 실어야 한다. 이름은 본문 서체(Gowun Batang)로 남는다.
  서브셋 목록은 `src/styles/ink-phrases.ts`의 `INK_PHRASES`와 `INK_GLYPHS`가 유일한 출처이고, `npm run font:subset`이 woff2와 `base.css`의 `unicode-range`를 함께 갱신한다.
- **이전 검토**: Gaegu 700(구글 폰트) → Nanum Pen Script(자간 +10%) 순으로 검토했으나, 최종적으로 KCC김환기체로 확정하며 자간 조정을 없앴다. 검토 과정은 프로토타입의 "모션·손글씨 레퍼런스" 보드에 남아 있다.

### 크기

| 요소 | 크기 | 굵기 |
|---|---|---|
| 화면 제목(h1) | 22~29px | 700 (Gowun Batang) |
| 본문·목록 항목 | 15~16px | 400 / 500 |
| 설명문 | 13~14px | 400 |
| 보조·날짜·타이머 | 12px | 400 |
| 단계 표시(n/7) | 12px | 400 |

---

## 3. 종이 · 배경

```css
.desk{ background: var(--desk); padding: 8px; }
.paper{
  background: var(--paper);
  border: 1px solid #DDD8CC;
  border-radius: 3px;
  box-shadow: 0 2px 10px rgba(35,32,28,0.06);
}
.margin-line{ /* 종이 왼쪽 세로 여백선 */
  width: 1px; background: rgba(179,64,43,0.22);
}
```

- 화면은 "책상(`--desk`) 위에 놓인 종이 한 장" 구조다. 바깥 여백 8px, 종이에 옅은 테두리와 그림자.
- 종이 왼쪽에는 노트 여백선(빨간 세로선, 22% 불투명도)이 있다.
- 카드는 종이보다 밝은 순백(`--card`)으로 올려서 떠 보이게 한다.
- 홈 화면 배경에만 `--rule` 가로 괘선(31~32px 간격)을 반복한다.

---

## 4. 모션 토큰

| 이름 | 시간 | 용도 |
|---|---|---|
| `write-on` | 300ms | 질문·결과 문장이 왼쪽에서 쓰이듯 나타남. 줄마다 80ms 지연 |
| `ink-draw` | 380ms | 밑줄·체크·동그라미·취소선. `stroke-dashoffset`으로 펜이 긋는 것처럼 |
| `settle` / `liftIn` | 220~260ms | 카드·후보·기준이 살짝 떠서 내려앉음 |
| `hilite` | 320ms | 형광펜, 화면당 최대 1곳 |
| `strike` | 420ms | 필수조건 탈락 취소선, 이어서 opacity 0.45 |

- 이징은 전부 `cubic-bezier(.22,.9,.24,1)`.
- 항목 간 지연 간격은 80ms.
- **한 화면의 진입 모션 전체는 900ms 안에 멈춘다.**
- 뒤로 가기는 모션을 역재생하지 않고 페이드로 끝낸다.
- `prefers-reduced-motion`에서는 전부 생략하고 최종 상태만 보여준다.
- 반복 루프·떠 있는 로딩·장식용 파티클은 쓰지 않는다.

---

## 5. 핵심 컴포넌트

| 컴포넌트 | 규칙 |
|---|---|
| 원형 번호(①②③) | Gowun Batang. 기본은 `--pen`. **선택지를 나란히 비교하는 화면**(결과·직접 조정해보기·내가 매긴 표)에서만 선택지 고유색(`--alt-n`)을 쓴다 |
| 진행 단계바 | 6칸(`FLOW` 길이를 따른다), 완료 구간 `--ink`, 미완료 `--border` |
| 확신도/만족도 선택 | 5칸, 선택 이하 칸은 `--accent` 채움, 이상 칸은 투명 테두리만 |
| 결과 적합도 막대 | 선택지 고유색(`--alt-n`). 앞선 쪽은 100%, 나머지는 `--alt-dim`(55%) |
| 순위 변동 | 순서가 바뀌면 줄이 서로 자리를 바꾸며 미끄러진다(420ms `--ease`). 텔레포트하면 무엇이 무엇을 제쳤는지 볼 수 없다 |
| 필수조건 탈락 | `--accent` 배지 유지, 텍스트에 `--pen` 취소선 |
| 결정 도장 | 원형, `--pen` 테두리, KCC김환기체로 "결정함" |
| 카드/섹션 | 배경 `--card`, 테두리 `--border`, radius 14~15px |
| 매트릭스 표 | SVG 한 장. 가로축 선택지(①②③ + 이름), 세로축 기준(이름 + 무게 %), 칸은 손글씨 점수, 기준마다 최고점에 `--pen` 손그림 동그라미. 동점이면 동그라미 없음. 적합도 막대는 넣지 않는다 |
| 순서 바꾸기 | 손잡이·화살표 없이 **줄을 길게 눌러(350ms) 끈다.** 들린 줄은 `--ink` 테두리에 그림자, 종이 띠처럼 -0.8° 기운다. 손가락이 먼저 8px 넘게 움직이면 스크롤로 넘긴다. 들린 뒤에만 문서 `touchmove`를 막는다(`passive: false`). 이름이 있는 줄은 평소 글자로 보이고 누르면 입력칸이 된다 — 입력칸 위에서 길게 누르면 글자 선택과 겹치기 때문이다. 키보드·보조기기는 Alt+↑/↓, 옮긴 결과는 `aria-live`로 읽어준다 |
| 하단 버튼 영역 | 다음으로 가는 버튼은 스크롤 밖 종이 아래쪽(`Paper`의 `footer`)에 붙는다. 위로 20px 종이색으로 번져 "아래에 더 있다"를 선 없이 알린다. 내용이 길어도 버튼이 잘리지 않는다 |
| 평가 점수 | 1~5 숫자 칸. 칸마다 말을 붙이지 않고 양 끝에만 **bad ↔ good** (`--soft`, 12px) |
| 민감도 눈금 ('자세히') | 손잡이 없는 눈금이다 — 만질 수 있는 슬라이더처럼 보이면 안 된다. `--ink` 눈금에 "지금", `--pen` 눈금에 손글씨 "여기부터 ①". 직접 움직이는 건 '직접 조정해보기'의 몫 |
| 상태 쪽지 (홈) | 알약 배지 대신 카드 위쪽 모서리에 비스듬히 붙인 포스트잇. 손이 가야 하는 상태("회고할 때", "진행 중")는 `--postit`, 나머지("회고 D-30", "확정됨", "돌아보니 …")는 `--chip-neutral` 종이. 카드마다 ±2°로 번갈아 기울인다. 다른 화면의 배지("통과", "느낌")는 그대로 알약 |
| 스티커 (에셋) | **그림 36가지**(결정·생각 11, 문구·다이어리 11, 일상·감성 14) — 흑백 먹선, 그림마다 어울리는 칠 하나(흰색·회색·도트·줄무늬·격자)를 정해 두고 외곽선 있음·외곽선 없음·선만 세 가지로 뽑는다. **라벨 5가지**(원형·꼬리표·티켓·말풍선·리본 배너)도 같은 세 가지. **포스트잇 5가지 × 감성 컬러 5색**, **마스킹테이프 10가지**(감성 컬러). 그림의 색은 `--ink`·`--card`·`--chip-neutral`·`--border`·`--rule-ink`만, 포스트잇·테이프는 위 '스티커 전용 감성 컬러'만. 결과를 정답처럼 보이게 하는 소재(별·1등·트로피)는 넣지 않는다. 레이어는 `외곽선 · 칠 · 무늬 · 먹선`(포스트잇은 `그림자 · 종이 · 무늬 · 접힌 모서리`)으로 나뉘고, 무늬는 SVG pattern이 아니라 실제 점·선 도형을 그림 모양으로 잘라 넣는다 — 피그마가 pattern 채우기를 제대로 못 읽는다. 원본은 `scripts/make-stickers.mjs` 하나 |
| 스티커 (화면) | 다이어리에 붙인 장식. 읽어주지 않고(`aria-hidden`) 눌리지 않으며 글자를 가리지 않는 자리에만 둔다. **홈**: 제목 옆 생각구름, 첫 카드 모서리에 분홍 도트 테이프, 맨 아래 손글씨 옆 새싹, 빈 홈에는 점선 상자 대신 갈림길 표지판. **고민을 적는 6단계와 회고·보관함·개인정보**: 상단바 오른쪽 빈자리에 한 장(연필·갈림길·하트·체크리스트·자물쇠·저울 / 달력·봉투·열쇠). **결과·자세히·조정해보기·내가 매긴 표·확정에는 붙이지 않는다** — 결론을 읽는 자리다. 쓰는 스티커만 `Sticker.tsx`에서 불러온다 |
| 목록 카드 | 제목은 띄어쓰기에서만 끊는다(`word-break: keep-all`). 척도 값은 숫자 대신 말로("마음 꽤 기욺", "돌아보니 잘한 선택") |
| 앱 아이콘 | 확정 화면의 도장 그대로 — 손글씨 `결정`을 세로 2단으로 쌓고 세로로 긴 타원(23.5×33)을 두른다. 격자는 108×108, 모티프는 안전 영역(중심에서 반지름 36) 안. 웹 파비콘은 원고지 바탕까지, **안드로이드는 단색 종이 + 모티프**(앞면만 흔드는 런처가 있어 종이 무늬를 앞면에 넣지 않는다) |
| 스플래시 | 책상색 바탕 가운데 도장 하나. `capacitor.config.ts`의 `backgroundColor`와 같은 색이라 이음매가 없다 |
| 소리 배지 | 상단 우측 알약형. 켜짐이면 `--accent` 테두리·배경 `--chip-accent`에 막대 3개가 움직임. 누르면 꺼지고 그 선택이 남는다 |

---

## 6. 화면 구성 (프로토타입 13개)

| 파일 | 화면 |
|---|---|
| `Main.dc.html` | 홈 · 결정 노트 |
| `Frame.dc.html` | 1 · 고민 한 문장 |
| `Alternatives.dc.html` | 2 · 후보 적기 |
| `Gut.dc.html` | 3 · 직감 봉인 |
| `Criteria.dc.html` | 4 · 기준 세 개 |
| `Must.dc.html` | 5 · 필수조건 |
| `Weight.dc.html` | ~~6 · 중요한 순서~~ — 2026-09-23 기준 화면으로 합침 |
| `Evaluate.dc.html` | 7 · 평가 |
| `Result.dc.html` | 결과 |
| `Why.dc.html` | 왜 이런 결과인지 |
| `Commit.dc.html` | 결정 확정 |
| `Review.dc.html` | 30일 뒤 회고 |
| `InkMotion.dc.html` | 모션·손글씨 검토 이력 (참고용, 앱 화면 아님) |
| `Palette.dc.html` | 컬러 팔레트 후보 A/B/C/D (참고용, 앱 화면 아님) |

프로토타입에 없이 앱에만 있는 화면:

| 화면 | 자리 | 왜 |
|---|---|---|
| 보관함 (`/settings`) | 홈 → 우상단 상자 아이콘 + "보관함" | 동기화를 사용자가 켜고 끄는 모델이라 필요 |
| 내가 매긴 표 (`/d/:id/matrix`) | 7 · 평가 → **결과 보기 직전** | 앱이 "②가 더 잘 맞습니다"라고 말하기 전에, 그 말이 무엇 위에 서 있는지를 먼저 보여준다. 사용자가 이미 적은 것만 모은다. **적합도는 그리지 않는다** — 결과를 미리 말하게 되므로 (2026-09-20 추가) |

---

## 7. 구현 전제

- Firebase Auth + Firestore 가정. 고민 본문은 기기 우선 저장, 동기화는 사용자 선택.
- 가중치는 순서 입력만 받고 내부에서 ROC 방식(SMARTER)으로 산출. 사용자에게 숫자를 직접 요구하지 않는다.
- 총점 숫자는 기본 화면에 노출하지 않는다. "적합도"와 "차이를 만든 기준"만 보여준다.
- 민감도는 그래프보다 자연어 문장을 먼저 보여준다.
- 사실/추정/느낌 태그는 결과 화면의 "남은 불확실성" 표시로 그대로 이어진다.

---

## 8. 변경 이력

| 일자 | 내용 |
|---|---|
| 2026-09-19 | 1차 시안 제작 (크림 종이 + Gowun Batang + Nanum Pen) |
| 2026-09-19 | Build on Notes 지시서(도트그리드·2px 테두리) 적용 시도 → 앱 전용 요구(필기 모션)와 충돌해 롤백 |
| 2026-09-19 | 사용자 요청으로 1차 레이아웃 유지, 색·서체만 Build on Notes 톤으로 교체 |
| 2026-09-19 | 종이색 밝게, 종이 외곽선 추가, 손글씨 Gaegu 700으로 교체 |
| 2026-09-19 | 구글 폰트 미로딩 확인 → woff2 서브셋 base64 임베딩으로 전환(외부 요청 제거) |
| 2026-09-19 | 손글씨 Nanum Pen Script + 자간 10%로 교체, 컬러 팔레트 A/B/C/D 후보 제시 |
| 2026-09-19 | 팔레트 A 확정, 대비 검사로 보조색 `#8A8478`→`#6E695C` 수정 |
| 2026-09-19 | 확신도 버튼 색 논의 끝에 강조색(`--accent` `#2F6A5E`)으로 통일 |
| 2026-09-19 | 손글씨 **KCC김환기체**로 최종 확정, 자간 조정 제거. 디자인 시스템 문서화 및 확정 |
| 2026-09-20 | 매트릭스 화면(내가 매긴 표) 추가 — 만년필 필기 모션, 손글씨 서브셋에 숫자 추가, 소리 토큰 신설 |
| 2026-09-20 | 매트릭스를 결과 **앞으로** 옮기고 적합도 줄 제거. '왜 이런 결과인지'의 기준별 막대는 중복이라 삭제. 순서 바꾸기를 포인터 이벤트로 교체(터치에서 안 되던 것) |
| 2026-09-21 | 앱 아이콘·스플래시를 `결정` 도장으로. Capacitor 기본 로고를 걷어내고 `scripts/icon-source.mjs` 한 곳에서 모든 크기를 뽑도록 함 |
| 2026-09-25 | 화면에 스티커를 붙임 — 홈은 넉넉히, 적는 단계는 상단바에 한 장, 결론을 읽는 화면은 비움 |
| 2026-09-25 | 스티커를 그림 36가지로 늘리고(무늬만 바꾼 변형은 없앰) 포스트잇·마스킹테이프를 감성 컬러로. '스티커 전용 감성 컬러' 5쌍 추가 |
| 2026-09-24 | 흑백 먹선 스티커 에셋 163장 (`npm run stickers`). 어디에 붙일지는 아직 정하지 않음 |
| 2026-09-24 | 홈 목록 배지를 포스트잇 쪽지로. `--postit` 토큰 추가(값은 `--hl`과 같음). '펜 자국'·'종이 한 장' 시안은 채택하지 않음 |
| 2026-09-23 | UI 검토 반영: '중요한 순서' 단계를 없애고 기준 줄을 길게 눌러 순서를 바꾸게 함(7→6단계). 하단 버튼 영역 신설. 빨강을 사용자의 손자국으로 되돌림(포커스·링크·상태 글자에서 제거). 보관함을 홈 우상단에. '자세히'의 가짜 슬라이더를 눈금으로. 평가 점수 양 끝 bad/good. 상단 "저장됨" 제거 |
| 2026-09-20 | 블록마다 붙던 작은 표제부 6곳 제거 — 라벨 자리에 문장을 넣고(A) 카드 테두리를 걷어냄(C). §9에 선택지 고유색 예외 추가, 순위 변동 모션 신설 |

---

## 9. 금지 목록

- ❌ 확신도·만족도에 강조색(`--accent`) 외의 새 색 쓰기
  - "앞선 선택지"는 2026-09-20부터 §1의 선택지 고유색을 쓴다. 그 다섯 색 밖으로 나가지 않는다.
- ❌ 손글씨 요소에 자간 조정 넣기
- ❌ 900ms를 넘는 진입 모션
  - 단 하나의 예외: **매트릭스 화면의 필기 모션**(최대 19초). 화면이 뜰 때마다 반복되는 모션이 아니라 결정당 한 번 보는 연출이라 다른 종류로 본다. 조건 — 건너뛰기를 항상 띄울 것, `prefers-reduced-motion`이면 그리지 않고 완성 상태만 보일 것, 소리는 끌 수 있고 그 선택이 남을 것.
- ❌ 총점 숫자를 기본 화면에 그대로 노출 (매트릭스의 적합도 줄도 `낮음/보통/높음`과 막대로만)
- ❌ 반복 루프 애니메이션 (레퍼런스 보드 제외)
- ❌ 다크모드
